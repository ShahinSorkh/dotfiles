Run `./install.zsh -h` for installation instructions.

Required packages:
	autojump
	cargo (rust package manager)
	composer (php package manager)
	direnv
	git
	ripgrep
	tmux
	vim
	yarn (nodejs package manager)
	zsh
